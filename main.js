function doubleAll(numbers) {
  const twiceOver = [];
    for (let i = 0; i < numbers.length; i++) {
      twiceOver.push(numbers[i] * 2);
    }
  return twiceOver;
}

function absoluteValues(maxim) {
  const abso = [];
  for (let i = 0; i < maxim.length; i++) {
    if (maxim[i] === )
  }
  {
    if (maxim[i] === maxim[i]) {
      abso.push(maxim[i])
    }
  }
  return abso;
}

function yelledGreetings(oldYeller) {
  //const shout = [];
  // for (let i = 0; i < oldYeller.length; )
}

function changeToInitials() {
}

function doubleOdd() {
}

function upperCaseFirstLetters() {
}

function add1ToLeft() {
}


module.exports = {
  doubleAll,
  absoluteValues,
  yelledGreetings,
  changeToInitials,
  doubleOdd,
  upperCaseFirstLetters,
  add1ToLeft,
}